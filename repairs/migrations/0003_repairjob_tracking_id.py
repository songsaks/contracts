# Generated by Django 5.2.7 on 2026-02-10 11:28

import uuid
from django.db import migrations, models


def gen_uuid(apps, schema_editor):
    RepairJob = apps.get_model('repairs', 'RepairJob')
    for row in RepairJob.objects.all():
        row.tracking_id = uuid.uuid4()
        row.save(update_fields=['tracking_id'])


class Migration(migrations.Migration):

    dependencies = [
        ('repairs', '0002_repairitem_final_cost'),
    ]

    operations = [
        # Step 1: Add field as nullable, no unique constraint yet
        migrations.AddField(
            model_name='repairjob',
            name='tracking_id',
            field=models.UUIDField(null=True),
        ),
        # Step 2: Fill each existing row with a unique UUID
        migrations.RunPython(gen_uuid, migrations.RunPython.noop),
        # Step 3: Alter to the final state (non-nullable, unique, with default)
        migrations.AlterField(
            model_name='repairjob',
            name='tracking_id',
            field=models.UUIDField(default=uuid.uuid4, editable=False, unique=True),
        ),
    ]
